function navbar() {
    // get element from DOM topnav and update to toggle the responsive class
    var x = document.getElementById("myTopnav");
    if (x.className === "topnav") {
      x.className += " responsive";
    } else {
      x.className = "topnav";
    }
}